[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "oop-space-shooter"
version = "1.0.0"
description = "Final Team Project for OOP Course"
dependencies = [
    "pygame"
]